name: musicare_audiosource_youtube_plugin
description: YouTube Audio Source Plugin test harness for MusicAre
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.10.0
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  serious_python: ^0.8.0
  serious_python_linux: ^0.8.0
  http: ^1.2.0
  path: ^1.9.0
  archive: ^3.6.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  path_provider: ^2.1.2
  path_provider_platform_interface: ^2.1.2
  plugin_platform_interface: ^2.1.8
